import{_ as e,c as s,o as a,V as t}from"./chunks/framework.BCxdY_ip.js";const v=JSON.parse('{"title":"Module Js.File","description":"","frontmatter":{},"headers":[],"relativePath":"api/ml/melange/Js-File.md","filePath":"api/ml/melange/Js-File.md"}'),n={name:"api/ml/melange/Js-File.md"},i=t(`<h1 id="module-js-file" tabindex="-1">Module <code>Js.File</code> <a class="header-anchor" href="#module-js-file" aria-label="Permalink to &quot;Module \`Js.File\`&quot;">​</a></h1><p>Bindings to File</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>type t = Js.file</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>type options = {</span></span></code></pre></div><p><code>type_ : string option;</code> A string representing the MIME type of the content that will be put into the file. Defaults to a value of &quot;&quot;.</p><p><code>endings : [ </code>transparent | <code>native ] option;</code> How to interpret newline characters (\\n) within the contents, if the data is text. The default value, transparent, copies newline characters into the file without changing them. To convert newlines to the host system&#39;s native convention, specify the value native.</p><p><code>lastModified : float option;</code> A number representing the number of milliseconds between the Unix time epoch and when the file was last modified. Defaults to a value of Date.now().</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>}</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>val options : </span></span>
<span class="line"><span>  ?type_:string -&gt;</span></span>
<span class="line"><span>  ?endings:[ \`transparent | \`native ] -&gt;</span></span>
<span class="line"><span>  ?lastModified:float -&gt;</span></span>
<span class="line"><span>  unit -&gt;</span></span>
<span class="line"><span>  options</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>val make : </span></span>
<span class="line"><span>  string Js.iterator -&gt;</span></span>
<span class="line"><span>  filename:string -&gt;</span></span>
<span class="line"><span>  ?options:options -&gt;</span></span>
<span class="line"><span>  unit -&gt;</span></span>
<span class="line"><span>  t</span></span></code></pre></div><p><code>make contents_array ~filename</code> creates a new file from an iterable object such as an Array, having ArrayBuffers, TypedArrays, DataViews, Blobs, strings, or a mix of any of such elements, that will be put inside the File. Note that strings here are encoded as UTF-8, unlike the usual JavaScript UTF-16 strings.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>val lastModified : t -&gt; float</span></span></code></pre></div><p><code>lastModified t</code> accesses the read-only property of the File interface, which provides the last modified date of the file as the number of milliseconds since the Unix epoch (January 1, 1970 at midnight). Files without a known last modified date return the current date.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>val name : t -&gt; string</span></span></code></pre></div><p>The <code>name t</code> read-only property of the File interface returns the name of the file represented by a File object. For security reasons, the path is excluded from this property.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>val size : t -&gt; float</span></span></code></pre></div><p><code>size t</code> returns the size of the File in bytes</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>val type_ : t -&gt; string</span></span></code></pre></div><p><code>type_ t</code> returns the MIME type of the file.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>val arrayBuffer : t -&gt; Js.arrayBuffer Js.promise</span></span></code></pre></div><p><code>arrayBuffer t</code> returns a Promise that resolves with the contents of the blob as binary data contained in a <code>Js.arrayBuffer</code>.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>val bytes : t -&gt; Js.uint8Array Js.promise</span></span></code></pre></div><p><code>bytes t</code> returns a Promise that resolves with a <code>Js.uint8Array</code> containing the contents of the file as an array of bytes.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>val slice : ?start:int -&gt; ?end_:int -&gt; ?contentType:string -&gt; t -&gt; t</span></span></code></pre></div><p><code>slice ?start ?end_ ?contentType t</code> creates and returns a new File object which contains data from a subset of the file on which it&#39;s called.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>val text : t -&gt; string Js.promise</span></span></code></pre></div><p><code>text t</code> returns a Promise that resolves with a string containing the contents of the file, interpreted as UTF-8.</p>`,27),p=[i];function o(l,c,d,r,h,g){return a(),s("div",null,p)}const b=e(n,[["render",o]]);export{v as __pageData,b as default};
